import './Cliente.scss'
import TableFlex from '@/components/TableFlex'

const Cliente = () => {
  return (
    <div className="cliente">
      <h1 className="cliente__title">Gerenciamento de Clientes</h1>
      <div>
        <TableFlex items={} remove={} update={} />
      </div>
    </div>
  )
}

export default Cliente
